-- No seed data required for blogger to boot. The single-row config
-- tables (site_settings, blog_builder_settings) are seeded inline in
-- schema.sql. Users, posts, keywords, and clusters are all created at
-- runtime through the app.
SELECT 1 WHERE FALSE;
